import { isValid, sendData, FormHandler } from '../src/functions'

describe('Tests', () => {
  // units
  describe('isValid', () => {})
  describe('sendData', () => {})
  // integration
  describe('FormHandler', () => {})
})
